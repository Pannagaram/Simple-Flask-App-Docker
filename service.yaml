apiVersion: flaskapp:v1
kind: Service
metadata:
  labels:
    app: flaskapp
  name: flaskapp
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5070
      protocol: TCP
  selector:
    app: flaskapp
